@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered rockets of the week</h2>
<h4>Clients preferences - just for you!</h4>


<div class="row">
    @foreach (var rocket in Model.PreferredRockets)
    {
        @Html.Partial("RocketsSummary", rocket)
    }
</div>